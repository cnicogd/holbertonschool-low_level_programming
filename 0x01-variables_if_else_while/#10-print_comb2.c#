#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main (void)
{
int a = 0;
while (a <= 99)
{
if (a < 10)
{
putchar((char) a / 10 + '0');
putchar((char) a % 10 + '0');
putchar('\n');
}
a++;
}
}
