#include "holberton.h"
/**
 * _strlen - Write a function that returns the length of a string.
 *
 * @s: parameter to be passed trhought the function.
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0 ; s[i] != '\0'; i++)
	{
	}
	return (i);
}
