#include "holberton.h"
/**
 * rev_string - reverse a string
 *@s: reverse string
 *Return: notrebloh
 */
void rev_string(char *s)
{
	int i, j, n;
	char orig[i];

	for (i = 0, j = n - 1; (i <= n - 1) && (j >= 0); ++i, --j)
	{
		s[j] = orig[i];
	}
}
